name: PikaOS Package Dev i386 build (Nvidia Ubuntu Forced 555)

on:
  push

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/pikaos-linux/pika-i386-package-container:latest
      volumes:
        - /proc:/proc
      options: --privileged -it

    steps:
    - uses: actions/checkout@v3
      with:
        submodules: true

    - name: Update apt cache
      run: apt-get update -y
    
    - name: Build i386 Package
      run: ./ubuntu-nvidia-drivers-mirror/makedriver.sh i386
    
    - name: Prepare nVidia driver archive
      run: ./prep-archives.sh
    
    - name: See for ourselves
      run: ./check-files.sh
